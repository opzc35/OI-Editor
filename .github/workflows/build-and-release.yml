name: Build and Pre-release

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyqt6 pyinstaller

      - name: Build single executable
        run: |
          pyinstaller --onefile --windowed main.py --name OI-Editor

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: OI-Editor-${{ matrix.os }}
          path: dist/OI-Editor*

  release:
    needs: build
    runs-on: ubuntu-latest
    if: (github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch') && github.repository == 'opzc35/OI-Editor'
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create download links
        run: |
          echo "Linux: https://github.com/opzc35/OI-Editor/releases/download/pre-release-${{ github.sha }}/OI-Editor-linux" > dist/download-links.txt
          echo "Windows: https://github.com/opzc35/OI-Editor/releases/download/pre-release-${{ github.sha }}/OI-Editor-windows.exe" >> dist/download-links.txt
          echo "MacOS: https://github.com/opzc35/OI-Editor/releases/download/pre-release-${{ github.sha }}/OI-Editor-macos" >> dist/download-links.txt

      - name: Create download links issue
        uses: peter-evans/create-issue-from-file@v5
        with:
          title: OI-Editor 预发布下载链接 ${{ github.sha }}
          content-filepath: dist/download-links.txt
          labels: pre-release,download
